let init = {links: ['usuarios', 'productos', 'contacto']}

let AppReducer = (prevState = init, action) => {
    switch(action.type){
        default :
            return prevState


    }

}

export default AppReducer